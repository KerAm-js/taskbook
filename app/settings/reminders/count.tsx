import { Header } from "@/shared";
import { Text, View } from "react-native";

export default function Count() {
  return (
    <View
      style={{
        flex: 1,
      }}
    >
      <Header title="count" />
    </View>
  )
}