import { Text, View } from "react-native";

export default function Interval() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Interval</Text>
    </View>
  )
}