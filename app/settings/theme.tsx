import { Text, View } from "react-native";

export default function Theme() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Theme</Text>
    </View>
  )
}